# Roadmap

本文件记录后续可以开发的方向。优先级会随着实际使用体验调整。

## Known Bugs

- 暂无已确认但未修复的问题。后续发现 bug 时先记录在这里，修复后移到 `CHANGELOG.md` 的 `Fixed`。
- 樱花草等歌曲，正常播放歌词对得上，但是手动移动进度条就不行了（对不上了）
- 随机播放的历史（`shuffleHistory`）记的是列表下标，刷新导致列表重排后「上一首」会回到错的歌；
  影响仅限随机模式下的上一首，不会崩溃。

## 上一批（2026-09-12 排定，已全部完成）

- ~~媒体键与系统「正在播放」集成~~（2026-09-12 完成：`NowPlayingCenter`，控制中心 / F7-F9 / AirPods）。
- ~~音乐库自动感知变化~~（2026-09-12 完成：`LibraryFolderWatcher` 目录级 DispatchSource + 后台增量 sync）。
- ~~UI 回归测试 + LrcParser 单测~~（2026-09-13 完成：`Tests/RunDatabaseTests/UILayoutTests.swift`、`LrcParserTests.swift`）。
- ~~日志~~（2026-09-13 完成：`AppLog` + 帮助菜单「导出诊断信息…」，`DiagnosticsReport`）。
- ~~播放队列与自建播放列表~~（2026-09-13 完成：队列 `PlayerWindowController_Queue`，自建列表 `PlayerWindowController_Playlists` + `PlaylistRepository`）。后续可做：列表内拖拽排序、队列侧栏。
- ~~无障碍~~（2026-09-13 完成：全部图标控件、滑杆、状态项补标签/数值描述，进度条方向键 seek）。
- ~~代码结构：播放态抽成独立 model~~（2026-09-13 完成：`NowPlayingModel`，菜单栏卡片已改为订阅；`MenuBarLyricsView` 早已删除，文档已清理）。

## 下一批（2026-09-13 排定）

1. ~~音乐文件元数据写入~~（2026-09-13 完成：`MetadataWriter` + `MetadataWriteService`，支持批量）。
2. ~~菜单栏悬停卡片弹出/收起加过渡动画~~（2026-09-13 完成：从右上角 0.6 倍过冲展开 + 淡入，可打断）。

## High Priority
交互体验优化：

- ~~播放里程碑弹窗 + 往年今日~~（2026-08-03 完成）：里程碑命中 10/50/100/300/500/1000 时
  播完当前这首再弹；往年今日按 12/6/3/1 个月取第一个有记录的跨度，数据满一年后自动升级为真·往年今日。
  两者共用同一个庆祝面板，各有独立开关。仍可做的后续：500/1000 这类大关卡加彩带、
  年度回顾（类似 Wrapped）、里程碑历史的查看界面。

界面美化（播放区视觉重心、封面等待 Medium 项）——以下 High Priority UI 项已于 2026-06-23 阶段性完成：
- ~~参考该音乐播放器的布局做沉浸模式~~（2026-08-03 完成，见 `assets/refs/photo_2026-07-27_23-46-39.jpg`）：
  ⌘⇧F 切换，大封面 + 大字左对齐歌词 + 底部控制行；日常的列表/歌词两列布局保持不变。
- ~~悬浮到菜单栏歌词弹出正在播放卡片~~（2026-08-03 完成，见 `assets/refs/photo_2026-07-27_23-49-05.jpg`）。
  双显示器下只有当前聚焦那块屏会弹出，属系统限制，已记入 `doc/ui.md`。

- ~~顶部工具栏精简~~：`NSToolbar` 收纳选择文件夹/刷新/搜索。
- ~~设置窗口~~：⌘, 打开；音乐库/Cookie/歌词/菜单栏歌词；支持移除文件夹。
- ~~窗口质感~~：`fullSizeContentView` + unified 工具栏 + 毛玻璃背景。
- ~~播放控制图标化~~：SF Symbol + 圆形播放键 + 自定义 `SeekSlider`。
- ~~歌词区美化~~：距离渐变、换行动画、动态留白居中。
- ~~曲目列表升级~~：双行单元格、悬停、播放行主题色底。
- ~~统一视觉规范~~：`EmptyStateView` 空状态；平铺毛玻璃布局（卡片/侧栏已回退）。
- ~~播放模式~~：顺序 / 单曲循环 / 随机；`player_state.playback_mode`（v3）。
- ~~定位正在播放~~：列表顶栏 `listNavigationStack`（`scope`）；左侧标题日后可换播放列表名。
- 如何使歌词始终在菜单栏的最左边（当我把在右边的应用程序关掉之后再开之后歌词被夹在中间了）

界面美化第二阶段（2026-07 提出，参考 macOS 26 Liquid Glass 方向与 Apple Music / Cider / Doppler 的做法）：

- ~~播放区信息补全~~（2026-07-08 完成：进度条行左侧封面 + 歌名/歌手；封面内嵌图 → 缓存/下载兜底 → 占位音符）。
- 歌词区 Apple Music 式排版：~~当前行字号放大 + 加字重、加大行距~~（2026-07-08 完成 26pt bold）；~~滚动换行 spring 弹性动画~~（2026-07-08 完成，过冲控制点曲线）；歌词字体尝试苹方 Medium / SF Pro Rounded 仍待做。
- ~~主题色氛围背景~~（2026-07-08 完成：`AmbientBackgroundView` 双色斑，封面主色驱动，切歌交叉淡入淡出，无封面回退纯毛玻璃）。
- Liquid Glass 适配（macOS 26）：2026-09-13 试过并已回退，暂不再做。把传输 pill 与模式 pill 换成 `NSGlassEffectView` 后，
  `.regular` 与 `.clear` 两种样式压在毛玻璃 + 氛围色斑上都比原来的 `quaternarySystemFill` 抢眼得多，
  成了界面里最重的一块。若日后重试，方向是给玻璃加低不透明度 `tintColor` 压住高光，或只在深色外观下启用。
- 微交互打磨：~~点击歌词行 seek 到该行时间~~、~~SeekSlider hover 轨道增高 + 预览时间气泡~~（2026-07-08 完成）；曲目行 hover 播放/暂停小按钮仍待做。
- 图标与空状态统一：~~设计正式 App 图标~~（2026-07-08 完成，见 `assets/`）；空状态插画统一 SF Symbol 分层配色仍待做。

~~音乐文件元数据写入~~（2026-07-08 提出，2026-09-13 完成：`MetadataWriter` + `MetadataWriteService`）：
三条约束都已落实（只补缺失字段、写前完整备份 + 原子替换、写完同步更新 `content_hash` 与 mtime/size）。
歌词真相仍是旁路 `.lrc`，App 显示逻辑不读内嵌歌词。仍可做的后续：

- wav / aiff 暂不支持（容器装不下封面与歌词）；写入前预览将要补哪些字段并允许逐项取消。
- 跨库同内容的副本只有被写入的那份标签会变，另一份保持原样；两者内容自此不同，完整重扫会各占一行。
- FLAC 写标签后 mtime/size 变化会让转码缓存失效并重新转码一次，旧缓存文件留在 Caches 里等系统回收。

## Medium Priority

- 专辑封面：~~下载或匹配封面~~、~~展示位置（播放区进度条行）~~（2026-07-08 完成自动下载 + 缓存展示）；预览后手动选择封面仍待做。
- ~~音量控制~~（2026-07-08 完成，`player_state.volume` schema v4）。
- ~~显示当前歌曲名、歌手~~（2026-07-08 完成于播放区信息块）；总时长已在进度条时间标签。
- 支持拖拽音乐文件夹到窗口加载。
- 多音乐库 UI：切换/管理多个已保存的音乐文件夹（设置中可列表查看与移除；切换活跃库仍待做）。
- ~~文件夹是不是应该有删除的功能~~（已在设置窗口实现）

## Low Priority

- ~~深色模式下的歌词颜色细化~~（2026-08-23 完成：新增外观三档切换，歌词当前行与列表播放行
  标题改用 `labelColor`；邻行透明度阶梯暂维持现状，观感有问题再调）。
- 支持读取音频内嵌歌词。
- 支持歌词文件编码自动识别。
- 支持更多歌词源，例如酷狗、酷我。
- 支持覆盖已有 `.lrc` 前确认。
- 歌词下载审计 log 的查看界面。

## Technical Improvements

- ~~自定义播放列表 UI~~（2026-09-13 完成）。
- ~~为 `LrcParser` 增加单元测试。~~（2026-09-13 完成）
- ~~为 `MusicLibrary` / `TrackRepository` 增加扫描与增量 sync 测试。~~（`test.sh` 已覆盖）
- ~~增加日志输出，方便排查播放失败和歌词匹配失败。~~（2026-09-13 完成）
- 将 FLAC 转码缓存改成后台任务，避免首次播放大文件时 UI 短暂卡住。
- 曲目数量很大时（>5000）为 tracks 表增加 FTS5 全文索引。
- `TrackMetadataReader` 改用 AVAsset 异步 load API，消除 macOS 13 弃用警告。

## 解决不了的

- 南山忆手动移动后歌词匹配不上 